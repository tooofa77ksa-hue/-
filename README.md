# شُعلة لغتي

منصة تعليمية عربية تفاعلية لمادة لغتي - الصف الثالث الابتدائي. تطبيق Full-Stack حقيقي:
- **/lughati/play** - لعبة تفاعلية للطالبات (بدون تسجيل دخول، قراءة فقط للمحتوى المنشور).
- **/lughati/teacher** - لوحة تحكم للمعلمة/الإدارة (تسجيل دخول إلزامي) لإدارة الأسئلة ونشرها.

أي تعديل تنشره المعلمة من `/lughati/teacher` ينعكس فورًا في `/lughati/play` عبر Firestore Real-time listeners، بدون أي تعديل على الكود.

> **ملاحظة:** هذا المستودع يضم الآن تطبيقين: **«إنجازي يحكي»** على الجذر (`/`) وهو التطبيق الرئيسي، و**«شُعلة لغتي»** كما هي تحت `/lughati`. الروابط القديمة (`#/play`) يُعاد توجيهها تلقائيًا.

## التقنيات

- **Frontend**: React + TypeScript + Vite، تقسيم حزم (Code splitting) بحيث لا يُحمَّل Phaser في `/teacher` ولا لوحة التحكم في `/play`.
- **اللعبة**: Phaser 3 (شخصية "دَمبل" وثلاث ألعاب: مهمة الصاروخ، كنز الدامبلنغ، البوابة السحرية).
- **Backend**: Firebase (Authentication, Cloud Firestore, Security Rules, Hosting). لا Cloud Functions لعدم وجود حاجة فعلية لعمليات خادمية أو أسرار.
- **الصوت**: AudioManager مركزي - صوت بشري عبر Slots جاهزة للاستبدال + مؤثرات صوتية مولَّفة عبر WebAudio (بدون ملفات خارجية).

## هوية بصرية

الألوان والشعار في Header/Footer مأخوذة حرفيًا من **دليل الهوية البصرية - وزارة التعليم (الإصدار 3، أكتوبر 2025)** المرفق، دون أي تعديل على ألوان الشعار أو نسبه أو إضافة تأثيرات (لا Glow ولا Shadow). ملف الشعار الرسمي: `public/assets/brand/ministry-logo.webp` (مُستخرج من الدليل نفسه). راجع `src/styles/theme.css` لبقية رموز الهوية (`--brand-*`).

خط الهوية الرسمي "Helvetica Neue W23 for SKY" مرخّص ولم يُضمَّن؛ تم استخدام "Tajawal" كبديل عربي مجاني مؤقتًا.

منطقة اللعبة نفسها (`--game-*` في theme.css) كرتونية زاهية منفصلة عن الهوية الرسمية، بانسجام لوني معها.

## إنجازي يحكي — ملف الإنجاز الرقمي (التطبيق الرئيسي)

«كل إنجاز… يحكي قصة تميّز» — منصة ملفات إنجاز لطالبات **الصف الرابع / 2** في **الابتدائية الخامسة والستين بعد المائة**: كل طالبة لها ملف مستقل بثيم تختاره، ولي أمرها يضيف مشاريعها وشهاداتها، والمعلمة تقيّم مشاريع مادتها بالنجوم وشارة التميّز، والمشرفة تدير كل شيء من لوحة الإدارة.

### المسارات

| المسار | لمن |
|---|---|
| `#/` | الصفحة العامة: عالم ثلاثي الأبعاد + بطاقات الطالبات |
| `#/student/{id}` | ملف إنجاز الطالبة |
| `#/login` | دخول ولي الأمر |
| `#/teacher` · `#/teacher/login` | بوابة المعلمات (مادتها فقط) |
| `#/admin` · `#/admin/login` | لوحة الإدارة (طالبات، معلمات، مواد، إعدادات) |
| `#/lughati/*` | **شُعلة لغتي** كما هي (نُقلت من الجذر لأن «إنجازي يحكي» يحتاج `/` و`/teacher`؛ `#/play` يُعاد توجيهه تلقائيًا) |

### قاعدة البيانات

كل مجموعات المنصة تحت `apps/injazi/…` (users, students, teachers, subjects, projects, evaluations, achievements, settings, activityLogs). العزل مقصود: قاعدة البيانات نفسها تخدم شُعلة لغتي التي تملك `students` و`users` بمخطّط مختلف وقواعد أمنية خاصة.

### الصلاحيات

ثلاثة أدوار (`admin` / `teacher` / `parent`) تُقرأ من `apps/injazi/users/{uid}` — لا من الواجهة. القواعد في `firestore.rules` و`storage.rules` تفرض: ولي الأمر يكتب في ملف ابنته فقط ولا يغيّر ترتيبه أو إخفاءه، المعلمة تكتب تقييم مادتها فقط وباسمها هي ولا تلمس ملفات الطالبات، والمشرفة وحدها تكتب ملفات الصلاحيات.

### التهيئة الأولى

```bash
# محليًا على المحاكيات
npm run emulator                       # طرفية أولى
FIRESTORE_EMULATOR_HOST=127.0.0.1:8080 \
FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099 \
GOOGLE_CLOUD_PROJECT=demo-injazi npm run injazi:seed

# على الإنتاج
GOOGLE_APPLICATION_CREDENTIALS=./serviceAccount.json \
GOOGLE_CLOUD_PROJECT=<projectId> \
INJAZI_ADMIN_EMAIL=... INJAZI_ADMIN_PASSWORD=... npm run injazi:seed
```

السكربت آمن للتشغيل المتكرر: يبحث قبل الإنشاء ولا يلمس أي مستند موجود.

### الاختبار

```bash
npm run test:rules   # 68 اختبار قواعد أمنية على المحاكي
npm run test:e2e     # 32 اختبار متصفح شامل (يتطلب المحاكي + npm run dev)
```

### قرارات هندسية تستحق المعرفة

- **الأداء**: القسم كله lazy. `three` (813KB) لا يُطلب على الجوال ولا الأجهزة الضعيفة ولا عند `prefers-reduced-motion` — والبديل المسطّح كامل الوظيفة. `lottie-web` يُطلب فقط عند أول لحظة نجاح/نجمة/تاج. البوابة في `src/injazi/lib/useCapability.ts`.
- **Lottie لا dotLottie**: نسخة `dotlottie-react` الحالية تجلب ~1MB من ThorVG WASM من CDN خارجي — غير مقبول على شبكة مدرسية. `lottie_light` يعطي النتيجة نفسها بلا WASM ولا شبكة.
- **Tailwind بلا Preflight**: التصفير العام كان سيكسر أنماط شُعلة لغتي القائمة.
- **الصور**: تُضغط وتُحوَّل إلى WebP في المتصفح قبل الرفع (صورة 4MB من الجوال تصبح ~150KB).
- **QR**: الزخرفة تحيط بالرمز ولا تلمسه، والهامش الأبيض كامل ومستوى تصحيح الخطأ H — الرمز مقروء من الشاشة ومن الورق.
- **RTL**: خصائص منطقية في كل الأنماط، و`inlineX()` لإزاحات الحركة الأفقية.

## بيانات الأسئلة (Seed)

الأسئلة الأولية في `scripts/seed.ts` مأخوذة من كراسة **"أستعد لأنافس - لغتي - ثالث ابتدائي"** المرفقة، وتغطي 11 من أصل 14 مهارة معتمدة (الملف المرفوع كان جزئيًا: 32 صفحة). المهارات الثلاث المتبقية (الرأي، التعبير الجمالي، نهاية مختلفة للنص) لم تُضَف لعدم ورودها في الملف - أضيفيها من لوحة المعلمة عند توفر بقية الكراسة.

## البدء السريع (محاكي محلي - بلا أي مشروع Firebase حقيقي وبلا أي Secret)

هذا هو المسار الموصى به للتطوير والاختبار؛ لا يحتاج تسجيل دخول ولا حساب Firebase حقيقي إطلاقًا:

```bash
npm install
cp .env.example .env
# افتحي .env وفعّلي قسم "الإمولاتور" المذكور بأعلى الملف (انسخي الأسطر المعلَّقة كما هي)

# نافذة 1: شغّلي المحاكيات (Firestore + Auth) وابقيها تعمل
npm run emulator

# نافذة 2: أدخلي بيانات أولية وأنشئي حساب معلمة تجريبي على المحاكي
FIRESTORE_EMULATOR_HOST=127.0.0.1:8080 FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099 npm run seed
FIRESTORE_EMULATOR_HOST=127.0.0.1:8080 FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099 \
  npm run create-teacher -- --username=Dalal --password=Dalal1234 --name="دلال"

# نافذة 2 أيضًا: شغّلي التطبيق (متصل بالمحاكي تلقائيًا لأن VITE_USE_FIREBASE_EMULATORS=true)
npm run dev
```

الآن `/lughati/teacher` تعمل باسم مستخدم `Dalal` / كلمة مرور `Dalal1234` (لا بريد إلكتروني ظاهر في الواجهة إطلاقًا - انظر "دخول المعلمة" أدناه) و`/lughati/play` تعرض الأسئلة الـ16 المزروعة - كل ذلك محليًا بلا أي اتصال بـ Firebase الحقيقي. واجهة فحص المحاكي: `http://127.0.0.1:4000`.

### اختبار قواعد الأمان تلقائيًا (Firestore Security Rules)

```bash
npm run test:rules
```

يشغّل هذا الأمر محاكيَي Firestore وAuth، ثم 43 اختبارًا آليًا (`tests/rules/firestore.rules.test.ts` عبر `@firebase/rules-unit-testing` وVitest) يغطي جميع السيناريوهات التالية على القواعد الفعلية في `firestore.rules`، ثم يطفئ المحاكيات تلقائيًا:

- مستخدم غير مسجَّل: لا يكتب، لا يقرأ سؤالًا غير منشور، يقرأ فقط `published==true && active==true`، ولا ينفّذ استعلامًا غير مقيَّد يكشف الكل.
- معلمة (`role: teacher`): تضيف/تعدّل/تحذف سؤالًا صالحًا، وتُرفض محاولاتها لحفظ سؤال بعدد اختيارات ≠ 4 أو بـ`correctAnswer` خارج 0-3 (عند الإنشاء وعند التعديل معًا).
- مستخدم مسجَّل دخول بلا صلاحية `teacher/admin`: تُرفض كل كتابة (إضافة/تعديل/حذف)، ولا يستطيع منح نفسه صلاحية عبر الكتابة على `users`.
- الطالبات/المجموعات (`students`/`groups`): قراءة وكتابة للمعلمة فقط، مرفوضة تمامًا لأي طرف آخر (بما في ذلك مستخدم مسجَّل بلا صلاحية).
- جلسات الاختبار (`testSessions`): أي طرف يفتح جلسة بمعرّفها المباشر (`get`) - هذا هو رابط الاختبار المُشارَك - لكن لا أحد غير المعلمة يستطيع تعداد كل الجلسات (`list`/query) أو إنشاء/تعديل/حذف جلسة.
- النتائج (`attempts`): كتابة محاولة واحدة متاحة بلا تسجيل دخول **فقط** إن ارتبطت بجلسة موجودة ونشطة وكانت الطالبة من مشاركي تلك الجلسة تحديدًا (لا انتحال، لا جلسات وهمية، لا جلسات متوقفة) مع تحقق أساسي من شكل البيانات؛ القراءة والحذف للمعلمة حصرًا، ولا قائمة علنية بأي نتيجة.

هذه الاختبارات تعمل أيضًا في CI تلقائيًا (`.github/workflows/ci.yml`، Job باسم `rules-test`) على كل Push/PR.

### الانتقال لاحقًا إلى مشروع Firebase حقيقي

عندما يتوفر مشروع Firebase فعلي، الخطوات المطلوبة منك فقط:

1. إنشاء المشروع في Firebase Console + تسجيل الدخول (`npx firebase login`).
2. تفعيل Authentication (Email/Password) وإنشاء قاعدة Cloud Firestore من الكونسول.
3. استبدال `demo-shualat-lughati` في `.firebaserc` بمعرّف مشروعك الحقيقي (أو `npx firebase use --add`).
4. تعبئة `.env` بقيم SDK الحقيقية من Project Settings، وتعيين `VITE_USE_FIREBASE_EMULATORS=false`.
5. نشر القواعد والفهارس: `npx firebase deploy --only firestore:rules,firestore:indexes`.
6. تشغيل `npm run seed` و`npm run create-teacher -- --username=Dalal --password=... --name="..."` (بدون متغيرات EMULATOR_HOST هذه المرة) - يتطلبان عندها إما `FIREBASE_TOKEN` (متغيّر بيئة من `firebase login:ci`، قابل للإلغاء من حسابك في أي وقت) أو `serviceAccountKey.json` حقيقيًا من Project Settings > Service Accounts (مُستبعد من git تلقائيًا، **لا تُنشئي ملفًا وهميًا بديلًا عنه**).
7. `npm run build && npx firebase deploy --only hosting`.

لم يُنشَر أي شيء إلى Production في هذا المستودع - هذا يتطلب حسابك.

## الأوامر

| أمر | الوصف |
|---|---|
| `npm run dev` | تشغيل بيئة التطوير |
| `npm run build` | بناء الإنتاج (typecheck + vite build) |
| `npm run lint` | فحص ESLint |
| `npm run typecheck` | فحص TypeScript فقط |
| `npm run emulator` | تشغيل Firebase Local Emulator Suite (Firestore + Auth) |
| `npm run test:rules` | اختبارات آلية لقواعد الأمان على المحاكي (18 سيناريو) |
| `npm run seed` | إدخال بيانات أولية (يعمل على المحاكي أو مشروع حقيقي، آمن لإعادة التشغيل) |
| `npm run create-teacher` | إنشاء/تحديث حساب معلمة أو إدارة (يعمل على المحاكي أو مشروع حقيقي) |

## بنية قاعدة البيانات (Firestore)

`users`, `questionSets`, `questions`, `skills`, `gameSettings`, `audioSettings`, `students`, `groups`, `testSessions`, `attempts` - التفاصيل الكاملة في `src/types/models.ts`. القواعد الأمنية في `firestore.rules`: لا كتابة عامة على أي مجموعة تحوي بيانات (طالبات/مجموعات/نتائج)، الطالبات يقرأن فقط الأسئلة المنشورة والمفعّلة. `students` تُخزّن **الاسم فقط** - بلا هوية أو جوال أو بريد أو أي معرّف آخر.

## دخول المعلمة (اسم مستخدم بسيط، بلا بريد إلكتروني ظاهر)

المعلمة تسجّل الدخول باسم مستخدم بسيط فقط (مثال: `Dalal`) وكلمة مرور - لا بريد إلكتروني ولا UID ولا أي مصطلح تقني ظاهر لها في `/teacher`. داخليًا فقط (Firebase Authentication لا يدعم إلا البريد/كلمة المرور)، `src/lib/usernameAuth.ts` يحوّل اسم المستخدم إلى بريد داخلي ثابت غير حساس (`username@teacher.shualat-lughati.internal`) - هذه ليست Secret، مجرد اصطلاح تسمية. لا تسجيل عام إطلاقًا؛ الحساب الحقيقي يُنشأ فقط عبر `npm run create-teacher -- --username=... --password=... --name="..."`.

## الطالبات، المجموعات، الاختبارات، النتائج

- **الطالبات** (`/teacher/students`): إضافة/تعديل/حذف/بحث بالاسم فقط، وتحديد مجموعة كل طالبة.
- **المجموعات** (`/teacher/groups`): إنشاء/إعادة تسمية/حذف (بدون حذف الطالبات)، إضافة/نقل/إزالة طالبات - الحجم المقترح نحو 5 طالبات (اقتراح فقط، بلا حد أقصى).
- **إنشاء اختبار** (`/teacher/tests`): اختبار فردي (طالبة واحدة) أو جماعي (مجموعة كاملة) - تختارين اللعبة والمهارة وعدد الأسئلة، فيُنشأ رابط اختبار (`#/play/t/<sessionId>`) تشاركينه مباشرة. الرابط يفتح مباشرة (لا تسجيل دخول) لكنه يعرض فقط أسماء طالبات هذه الجلسة تحديدًا (لا كل الطالبات) - انظر `firestore.rules` (`testSessions`: `get` عام، `list` للمعلمة فقط).
- **اللعب العام مقابل الاختبار الرسمي**: `/play` العادي بلا اسم وبلا حفظ إطلاقًا (كما كان). `/play/t/<sessionId>` اختبار رسمي مرتبط بجلسة، يحفظ محاولة كاملة (`attempts`) عند الانتهاء، بما في ذلك **لقطة** (Snapshot) لنص كل سؤال واختياراته والإجابة الصحيحة وقت المحاولة تحديدًا - تعديل السؤال لاحقًا من `/teacher` لا يغيّر أي نتيجة تاريخية محفوظة.
- **النتائج** (`/teacher/results`): بحث/تصفية حسب الطالبة/المجموعة/اللعبة/المهارة، إحصاءات (المتوسط/الأعلى/الأدنى/متوسط كل مجموعة/الأسئلة الأكثر خطأً/مهارات تحتاج مراجعة)، وتفصيل كل محاولة سؤالًا سؤالًا.
- **تصدير Excel احترافي**: زر "تصدير Excel" في صفحة النتائج يولّد ملف `.xlsx` حقيقي (مكتبة `exceljs`، مُحمَّلة كسوليًا في `/teacher` فقط - لا تصل إطلاقًا لحزمة `/play`) بخمس أوراق: ملخص النتائج، تفاصيل الإجابات، نتائج المجموعات، تحليل الأسئلة، الطالبات والمجموعات - جميعها RTL، برأس مجمَّد (Freeze) وتصفية تلقائية (AutoFilter) وعرض أعمدة مناسب. اسم الملف: `نتائج_لغتي_165_YYYY-MM-DD.xlsx`.

## إضافة نمط لعبة جديد

الألعاب Modular: أنشئي فئة ترث `BaseGameScene` (في `src/game/modes/`) وتنفّذ نفس عقد أحداث `gameBus`، ثم سجّليها في `src/game/modes/registry.ts` مع تسمية عربية وقيمة جديدة في نوع `GameMode` بـ `src/types/models.ts`. لا حاجة لتغيير Firestore أو Security Rules أو لوحة المعلمة.

## الصوت البشري

راجعي `public/audio/voice/README.md` لأسماء ملفات الصوت الثمانية المطلوبة. الكود يعمل بدونها (يكتفي بالمؤثر الصوتي المولَّف)، ويلتقطها تلقائيًا فور إضافتها بدون أي تعديل على الكود.

## الأمان

- Firestore Security Rules حقيقية (`firestore.rules`) - انظر التعليقات بداخلها.
- Rules مُختبَرة آليًا على Firebase Local Emulator Suite عبر `npm run test:rules` (18 اختبار، تعمل أيضًا في CI) - وليست ادّعاءً بلا تحقق.
- لا Secrets في الواجهة الأمامية - فقط مفاتيح Firebase العامة عبر `.env` (آمنة للعميل، الحماية الفعلية بالـSecurity Rules).
- `dependabot.yml` لتحديثات الاعتماديات، `codeql.yml` لفحص CodeQL (JavaScript/TypeScript)، و`ci.yml` يشغّل `npm audit`.
- لا تُجمع أي بيانات شخصية عن الطالبات (لا حساب، لا اسم، لا هوية).

## الفوتر

نص الفوتر ثابت حسب المتطلبات في `src/components/BrandFooter.tsx` ولا يجوز إضافة أي نص بعد سطر "تصميم المعلمة".
