# CLAUDE.md

إرشادات العمل في هذا المستودع.

## المشروع باختصار

مستودع يضم **تطبيقين منفصلين** يُنشران معًا عبر Firebase Hosting:

1. **«شُعلة لغتي»** (`src/`) — منصة تعليمية عربية. React 18 + TypeScript + Vite + Phaser،
   خلفيتها Firebase (Auth + Firestore). المسارات: `/` · `/play/*` · `/teacher/*`.
2. **خطة تحسين النهروان** (`public/nahrawan-plan.html`) — صفحة HTML مستقلة بلا بناء،
   خلفيتها **Supabase**. لا تتشارك أي كود أو بيانات مع التطبيق الأول.

## أوامر أساسية

```bash
npm run dev          # خادم التطوير
npm run typecheck    # فحص الأنواع
npm run lint         # التدقيق (--max-warnings 0)
npm run build        # البناء
npm run test:rules   # اختبارات قواعد Firestore (يحتاج محاكي)
npm run emulator     # محاكي Auth + Firestore
```

## سير العمل المطلوب

- **استخدم مهارة `arabic-web-studio`** في أعمال الواجهة والداشبورد ذات الصلة.
- **اقرأ `docs/PROJECT_STATE.md`** في بداية العمل، وطابق ما فيه مع الملفات الفعلية قبل الاعتماد عليه.
- **اتبع `docs/DESIGN_SYSTEM.md`** عند أي تعديل تصميمي. لا تكتب قيم ألوان أو خطوط مباشرة في
  المكوّنات — استخدم الـ tokens في `src/styles/theme.css`.
- **راجع `docs/DATA_MODEL.md`** قبل أي تغيير في البيانات أو الصلاحيات.
- **حدّث `docs/PROJECT_STATE.md` و`docs/TEST_REPORT.md`** بعد إنجاز كل مرحلة وقبل إنهاء العمل.
  سجّل الحالة الحقيقية فقط؛ لا تدوّن فحصًا لم يُشغَّل.
- **حافظ على قرارات المستخدمة السابقة** ولا تنقض تصحيحًا سابقًا، مع الاستجابة الكاملة لتعليماتها الجديدة.

## قواعد خاصة بهذا المستودع

- الهوية البصرية موجودة في **موضعين مستقلين**: `src/styles/theme.css` و`public/nahrawan-plan.html`.
  أي تغيير في الهوية يُطبَّق في الموضعين.
- نصوص الهوية (اسم المنصة، الوصف، اسم المدرسة، أسماء المديرة والوكيلة) قابلة للتعديل من لوحة
  المعلمة عبر `gameSettings.branding`. **لا تضعها hard-coded في الكود.**
- الصلاحيات تُفرض في `firestore.rules` لا في المتصفح. `?mode=admin` في صفحة النهروان تبديل
  واجهة فقط ولا يمنح أي صلاحية.
- لا تُرقِّ `eslint` أو `glob` ترقية عمياء — إعداد ESLint الحالي بصيغة legacy وستنكسر.
- خط الهوية الرسمي `Helvetica Neue W23 for SKY` مرخّص وغير مضمَّن؛ `Tajawal` بديل معتمد.
  لا تستبدله دون قرار صريح.
