# تعليمات لكلاود كود في هذا المستودع

راجعي `README.md` للتفاصيل الكاملة عن المشروع (شُعلة لغتي - منصة تعليمية عربية، React + TypeScript + Vite + Firebase).

## سياسة الأمان (إلزامية على كل تعديل)

قبل اعتبار أي مهمة برمجية منتهية في هذا المستودع:

1. راجعي أي كود جديد أو معدَّل بحثًا عن ثغرات أمنية شائعة (OWASP Top 10) تحديدًا:
   - تسريب مفاتيح/أسرار (Firebase config العام مسموح، لكن أي مفتاح خاص أو Service Account ممنوع في الكود).
   - كسر قواعد `firestore.rules` (قراءة/كتابة غير مصرَّح بها) - أي تعديل على القواعد يجب تشغيل `npm run test:rules` بعده والتأكد من نجاح الاختبارات الـ34.
   - حقن (XSS) عبر عرض محتوى غير منقّى في الواجهة، أو أي استخدام لـ `dangerouslySetInnerHTML`.
   - تسريب بيانات الطالبات (الاسم فقط مسموح تخزينه - لا هوية، لا جوال، لا بريد) - أي حقل جديد يخزّن بيانات طالبة يحتاج تبريرًا صريحًا.
   - أي منطق مصادقة/صلاحيات جديد (خصوصًا في `usernameAuth.ts` أو التحقق من دور `teacher`/`admin`).
2. إذا وجدتِ ثغرة فعلية (سواء كانت من كودك الجديد أو موجودة مسبقًا وصادفتِها أثناء العمل)، **نبّهي المستخدمة صراحة في ردك** - لا تُصلحيها بصمت ولا تتجاهليها، واذكري: أين الثغرة، ولماذا هي مشكلة، والإصلاح المقترح.
3. لأي تغيير غير بسيط (منطق مصادقة، Security Rules، معالجة مدخلات مستخدم، رفع ملفات)، شغّلي مهارة `security-review` قبل تسليم العمل، وليس فقط عند الطلب المباشر.
4. لا تُعطّلي أو تتجاهلي `npm run lint` أو `npm run test:rules` أو أي فحص CI موجود (`ci.yml`, `codeql.yml`, `dependabot.yml`) لتسريع التسليم.

هذه السياسة تنطبق على أي عمل في هذا المستودع تحديدًا، بغض النظر عمّن يطلب المهمة أو كيف صيغت.
